{
  "contract_id": "builder-basic-safe-divide",
  "objective": "Implement safe_divide in calculator.py.",
  "read": [
    "calculator.py",
    "tests/test_calculator.py"
  ],
  "allowed_to_modify": [
    "calculator.py"
  ],
  "forbidden": [
    ".codegen-contract/**",
    ".opencode/**",
    "opencode.json",
    "tests/**"
  ],
  "requirements": [
    { "id": "R1", "statement": "Return normal Python division as a float.", "kind": "change", "verification": "automated" },
    { "id": "R2", "statement": "Raise ValueError with the exact message 'divisor must not be zero' when the divisor is zero.", "kind": "change", "verification": "automated" },
    { "id": "R3", "statement": "Preserve the public function signature.", "kind": "preserve", "verification": "manual" }
  ],
  "verification": {
    "checks": [
      { "id": "C1", "covers": ["R1", "R2"], "command": "PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s tests -v" }
    ],
    "invariants": [
      "Only calculator.py changes.",
      "The contract, gate, tests, and OpenCode configuration remain unchanged."
    ]
  },
  "response": [
    "status",
    "changed files",
    "self-check result",
    "blockers"
  ]
}
