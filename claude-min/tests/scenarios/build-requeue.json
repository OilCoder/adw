{
  "fixture": "parallel-basic",
  "models": {
    "max_models_per_item": 1
  },
  "script": {
    "builder": {
      "stats-mean": [
        {
          "files": {},
          "steps": 2,
          "text": "BLOCKED\nnone\nnot run\nmissing decision"
        },
        {
          "files": {
            "stats.py": "def mean(values):\n    if not values:\n        raise ValueError(\"values must not be empty\")\n    return sum(values) / len(values)\n"
          }
        }
      ],
      "geometry-area": [
        {
          "files": {
            "geometry.py": "def rect_area(width, height):\n    if width < 0 or height < 0:\n        raise ValueError(\"sides must not be negative\")\n    return float(width * height)\n"
          },
          "sleep": 10
        }
      ],
      "report": [
        {
          "files": {
            "report.py": "from stats import mean\nfrom geometry import rect_area\n\n\ndef summary(values, width, height):\n    return f\"mean={mean(values)} area={rect_area(width, height)}\"\n"
          },
          "tools": [
            "read",
            "read",
            "write",
            "edit"
          ]
        }
      ]
    }
  },
  "runs": [
    {
      "run": [
        "build",
        "--parallel",
        "3"
      ],
      "during": [
        {
          "after": 5,
          "edit": {
            ".codegen/contracts/stats-mean/contract.json": "{\n  \"objective\": \"Implement mean in stats.py; raise ValueError('values must not be empty') on an empty list.\",\n  \"read\": [\n    \"stats.py\",\n    \"tests/test_stats.py\"\n  ],\n  \"allowed_to_modify\": [\n    \"stats.py\"\n  ],\n  \"protected\": [\n    \"tests/**\"\n  ],\n  \"requirements\": [\n    {\n      \"id\": \"R1\",\n      \"statement\": \"Implement mean in stats.py; raise ValueError('values must not be empty') on an empty list.\"\n    },\n    {\n      \"id\": \"R2\",\n      \"statement\": \"Preserve the public signature.\"\n    },\n    {\n      \"id\": \"R3\",\n      \"statement\": \"Return a float.\"\n    }\n  ],\n  \"gate\": \"bash .codegen/contracts/stats-mean/gate.sh\"\n}\n"
          },
          "run": [
            "build",
            "--resume",
            "--only",
            "stats-mean"
          ]
        }
      ]
    }
  ]
}
