# las-standard

## Answer

In LAS 2.0 the null value is declared in the `~W` section with the `NULL.` mnemonic, usually `-999.25`. Readers replace it with a missing value before any computation…

*Report cut at step 30: the summary and the numeric cases are missing.*
