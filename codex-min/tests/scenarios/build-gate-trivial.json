{
  "fixture": "builder-basic",
  "pre": {
    "calculator.py": "def safe_divide(dividend: int, divisor: int) -> float:\n    \"\"\"Divide two integers, rejecting division by zero.\"\"\"\n    if divisor == 0:\n        raise ValueError(\"divisor must not be zero\")\n    return dividend / divisor\n"
  },
  "script": {
    "builder": {
      "safe-divide": [
        {
          "files": {
            "calculator.py": "def safe_divide(dividend: int, divisor: int) -> float:\n    \"\"\"Divide two integers, rejecting division by zero.\"\"\"\n    if divisor == 0:\n        raise ValueError(\"divisor must not be zero\")\n    return dividend / divisor\n"
          },
          "tools": [
            "read",
            "edit"
          ],
          "steps": 5,
          "text": "DONE\ncalculator.py\ngate PASS\nnone"
        }
      ]
    }
  },
  "runs": [
    [
      "build",
      "--parallel",
      "1"
    ]
  ]
}